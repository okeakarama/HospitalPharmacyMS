@model IEnumerable<HPMS.Models.Drug>

@{
    ViewBag.Title = "dispensary";
}


<section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box box-info">
                @*<div class="box-header">

                    </div>*@

                <div class="box-body">
                    @*<h2 class="box-title" style="font-family:Britannic;">Drug List</h2>*@
                    <h1>DRUG DISPENSRY</h1>

                    @*@if (TempData["save"] != null)
                    {
                        <h3 class="text-success">@TempData["save"]</h3>
                    }
                    else if (TempData["save"] == null)
                    {
                        <h3 class="text-danger">NO NEW DRUG ADDED</h3>
                    }

                    <p>
                        <button class="btn">
                            @Html.ActionLink("ADD NEW DRUG", "createdrug")
                        </button>
                    </p>*@

                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th scope="col">
                                    Name
                                </th>
                                <th scope="col">
                                    Price(N)
                                </th>
                                <th scope="col">
                                    Description
                                </th>
                                <th scope="col">
                                    Quantity
                                </th>
                                <th scope="col">
                                    Rack
                                </th>
                                <th scope="col">
                                    Dispense
                                </th>
                            </tr>
                        </thead>

                        <tbody>

                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td scope="row">
                                        @Html.DisplayFor(modelItem => item.Name)
                                    </td>
                                    <td scope="row">
                                        @Html.DisplayFor(modelItem => item.Price)
                                    </td>
                                    <td scope="row">
                                        @Html.DisplayFor(modelItem => item.Description)
                                    </td>
                                    <td scope="row">
                                        @Html.DisplayFor(modelItem => item.Quantity)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Rack)
                                    </td>
                                    <td scope="row">
                                        @Html.ActionLink("dispense", "dispense", new { id = item.DrugID })
                                    </td>
                                </tr>
                            }

                        </tbody>

                    </table>

                    @*Add bootstrap pagination to this page*@

                </div>
            </div>
        </div>
    </div>
</section>




























@*<section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box box-info">
                <div class="box-header">

                    </div>

                <div class="box-body">
                    <h2>dispensary</h2>

                    <p>
                        @Html.ActionLink("Create New", "Create")
                    </p>
                    <table>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Price)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Quantity)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Rack)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Price)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Quantity)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Rack)
                                </td>
                                <td>
                                    @Html.ActionLink("dispense", "dispense", new { id = item.DrugID })
                                </td>
                            </tr>
                        }

                    </table>
                </div>
            </div>
        </div>
    </div>
</section>*@